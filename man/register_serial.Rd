% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registration.R
\name{register_serial}
\alias{register_serial}
\title{Register Serialization Configuration}
\usage{
register_serial(class, package = NULL, sfunc = NULL, ufunc = NULL)
}
\arguments{
\item{class}{a character string (or vector) of the class of object custom
serialization functions are applied to, e.g. \code{'ArrowTabular'} or
\code{c("torch_tensor", "ArrowTabular")}.}

\item{package}{a character string (or vector) naming the package that should
be loaded before deserializing objects for each class. May be inferred when
\code{sfunc} and \code{ufunc} both come from the same package namespace.}

\item{sfunc}{a function (or list of functions) that accepts a reference
object inheriting from \code{class} and returns a raw vector.}

\item{ufunc}{a function (or list of functions) that accepts a raw vector and
returns a reference object.}
}
\value{
Invisible \code{NULL}.
}
\description{
Registers a serialization configuration for custom serialization and
unserialization of reference objects. Once registered, the configuration is
applied automatically by \code{\link[=serialize]{serialize()}}, \code{\link[=unserialize]{unserialize()}}, \code{\link[=save_rds]{save_rds()}} and
\code{\link[=read_rds]{read_rds()}}. During deserialization, sakura loads the recorded package name
inline before dispatching to the registered unserializer, allowing packages
to self-register from \code{.onLoad()} or a package load hook.
}
